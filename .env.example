# Used by docker-compose.yml. Copy to .env and adjust as needed.

POSTGRES_DB=assignment_system
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

JWT_KEY=REPLACE_WITH_LONG_RANDOM_SECRET_AT_LEAST_32_CHARS
JWT_ISSUER=AssignmentSystem
JWT_AUDIENCE=AssignmentSystemClient
JWT_EXPIRY_MINUTES=60

NEXT_PUBLIC_API_URL=http://localhost:5000
