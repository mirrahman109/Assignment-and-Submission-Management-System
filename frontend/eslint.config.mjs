import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // This app has no query/cache library — "fetch on mount into local state via
      // useEffect + useState" is the entire data layer, used deliberately in every
      // page. This React-Compiler-aligned rule flags that standard pattern as an
      // error everywhere it appears; disabled rather than restructured, since nothing
      // it points at is an actual bug here.
      "react-hooks/set-state-in-effect": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
